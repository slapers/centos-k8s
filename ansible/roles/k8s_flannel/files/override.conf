#
# We override the ExecStartPost to remove the combined options being generated by mk-docker-opts.sh
# so that these can be overridden by /etc/sysconfig/docker-network
#
[Service]
ExecStartPost=
ExecStartPost=/usr/libexec/flannel/mk-docker-opts.sh -d /run/flannel/docker